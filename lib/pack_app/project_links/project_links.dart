import 'package:flutter/material.dart';

class ProjectLinksScreen extends StatelessWidget {
  const ProjectLinksScreen({super.key});

  @override
  Widget build(final BuildContext context) {
    return const Placeholder();
  }
}
